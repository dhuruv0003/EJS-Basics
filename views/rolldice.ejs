<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roll Dice</title>
</head>
<body>
    <!-- <h1>Dice gave value: <%= Math.floor(Math.random()*6) +1 %></h1> -->
    <!-- now using the key passed in res.render() -->

    <h1>Dice gave a value: <%= diceVal %></h1>
    
    <% if(diceVal==6) { %>
        <h2>Nice! you got  <%= diceVal %>, Roll dice again</h2>
    <% } %>

</body>
</html>

<!-- render below code in h1 above using <% %>
if (diceVal==6){
    <h2>Nice! Roll dice again</h2>
} -->